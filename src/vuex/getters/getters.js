export default { // getters = computed properties
    PRODUCTS(state) {
        return state.products;
    },
    PRODUCT(state) {
        return state.product;
    },
    CART(state) {
        return state.cart;
    },
}